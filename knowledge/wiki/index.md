# Knowledge Index

Seed pages:
- `patterns/` for reusable bug classes and anti-patterns
- `programs/` for bounty program scope and constraints
- `findings/` for validated findings and near-misses
- `playbooks/` for repeatable hunt workflows
