delegation:
  max_concurrent_children: 3
  max_spawn_depth: 2
  orchestrator_enabled: true
  max_iterations: 24
  child_timeout_seconds: 900
  # provider: set-via-hermes-setup
  # model: set-via-hermes-setup
