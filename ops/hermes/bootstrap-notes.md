# Why this bootstrap is conservative

Hermes configuration evolves quickly. This repo only ships a small, documented delegation block plus portable skills.

The bootstrap command copies skills and a config template, but does not overwrite your main Hermes config.
