# Program Profiling

Build one concise page per target program:
- in-scope assets
- exclusions
- rate limits
- safe validation notes
- likely high-value surfaces

Repo helper:
```bash
python -m bughunter_hive.cli program-init --name "<program>" --platform web2 --scope-domain app.example.com
```
