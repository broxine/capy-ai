# Passive Recon

Preferred worker: Recon Scout.

Command:
```bash
python -m bughunter_hive.cli recon --target <url> --program <name> --scope-domain <domain>
```

Output contract:
- JSON report path
- header observations
- robots/security policy discovery
- caveman summary
